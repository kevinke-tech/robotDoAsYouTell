These files are archived legacy examples kept for reference only.

- They are intentionally not part of the runtime `skills/` registry.
- They may include domain-specific defaults or historical prompt experiments.
- Do not import or auto-load them in production paths.
