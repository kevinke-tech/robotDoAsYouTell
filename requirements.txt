# vox backend (FastAPI)
# Phase 1: voice loop + camera + proxy probe
fastapi>=0.110.0
uvicorn>=0.25.0
python-dotenv>=1.0.0
httpx>=0.27.0
websockets>=12.0
numpy>=1.24.0
pydantic>=2.0.0
pillow>=10.0.0

# ASR (reused from vui)
# FunASR doesn't declare torch as a hard dep — install explicitly.
# Plain `torch` from PyPI gives the CPU wheel on Linux when no CUDA is detected.
torch>=2.0
torchaudio>=2.0
funasr>=1.0.0

# Anthropic SDK (proxy probe + planner in later phases)
anthropic>=0.40.0

# Phase 4: background runner — perceptual hash frame pre-filter
imagehash>=4.3.0

# Phase 3: Playwright browser body
# After install: `playwright install chromium` (~150 MB download).
# China users: set PLAYWRIGHT_DOWNLOAD_HOST=https://registry.npmmirror.com/-/binary/playwright
playwright>=1.40.0

# Phase 6: skill synthesizer
claude-agent-sdk>=0.1.0
cursor-sdk
