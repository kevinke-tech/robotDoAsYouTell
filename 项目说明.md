# vox 项目技术方案与原理详解

## 一、项目定位

**vox 是一个可自我扩展的语音视觉智能体**。它接收用户的自然语言指令（说话或文字），通过摄像头观察用户与环境，然后**调用已有技能完成任务**，或在没有相应技能时**实时让 Claude 写一段新代码来扩展自己的能力**。新技能会立即可用，下次再被请求时无需重新生成。

它与传统语音助手最本质的区别：**能力清单不是开发者预先写死的，而是在与用户对话中长出来的**。

---

## 二、整体架构

```
┌────────────── 浏览器前端 ──────────────┐
│  麦克风 ─ VAD ─→ /asr → 文本             │
│  摄像头 ─ 每次说话快照 ─→ /plan 携带图片  │
│  扬声器 ←─ /tts ←──────────────────────  │
│  WebSocket  ─→ /ws/output  长时事件推送  │
│           ─→ /ws/frames   视觉监视帧流  │
└────────────────────┬───────────────────┘
                     │
                     ▼
┌────────── 后端 FastAPI :5001 ──────────┐
│                                         │
│   /asr        FunASR (本地 CPU)        │
│                                         │
│   /plan       规划器(Claude Opus 4.7) │
│   ┌────────────────────────────────┐   │
│   │ 四工具路由:                    │   │
│   │  chat / call_skill /           │   │
│   │  synthesize_one_shot /         │   │
│   │  synthesize_background         │   │
│   └────────────────────────────────┘   │
│                                         │
│   /tts        Volcano TTS              │
│                                         │
│   技能库   ─→  Dispatcher              │
│   ┌──────┐    ┌─────────────────┐     │
│   │skills│ ─→ │ 调用一次性技能   │     │
│   │ */.py│    │ 触发后台技能注册 │     │
│   └──────┘    └─────────────────┘     │
│      ▲                ▲                │
│      │ 热加载         │                │
│      │                │                │
│   合成器  ◄────  规划器决定合成        │
│   ┌──────────────────────────┐         │
│   │ Claude Agent SDK 子进程  │         │
│   │ 受限沙箱目录             │         │
│   │ 烟雾测试 + AST 校验      │         │
│   │ 通过则升级到 skills/     │         │
│   └──────────────────────────┘         │
│                                         │
│   后台运行器 BackgroundRunner          │
│   ┌──────────────────────────┐         │
│   │ 定时器 (asyncio.sleep)   │         │
│   │ 视觉监视器:               │         │
│   │  pHash 去重 → Opus 视觉  │         │
│   │  判断匹配 → 提示用户     │         │
│   └──────────────────────────┘         │
│                                         │
│   浏览器主机 BrowserHost               │
│   ┌──────────────────────────┐         │
│   │ Playwright 持久化 Chromium│         │
│   │ 共享 Context，按需新建页 │         │
│   └──────────────────────────┘         │
└────────────────────────────────────────┘
```

---

## 三、各模块职责

| 模块 | 文件 | 职责 |
|---|---|---|
| 前端 | `frontend/` | 录音、VAD、摄像头预览、WebSocket、语音播放、语音播报前的提示音 |
| 服务器 | `server.py` | FastAPI，路由各 HTTP/WS 端点，挂载全局运行时 |
| 规划器 | `planner.py` | 调用 Claude（带视觉），用 `tool_use` 输出四选一决策 |
| 调度器 | `dispatcher.py` | 扫描 `skills/`，加载注册表，运行一次性技能（超时+异常隔离） |
| 后台运行器 | `background.py` | 管理定时器和视觉监视器的生命周期 |
| 视觉触发判断 | `trigger_check.py` | 给定帧 + 触发描述，调用 Claude 视觉返回 `{is_match, reason}` |
| 帧过滤 | `frame_filter.py` | 感知哈希去重，未变化的帧不送 Claude |
| 浏览器主机 | `browser.py` | Playwright 生命周期，一个共享 BrowserContext |
| **合成器** | `synthesizer.py` | **动态生成技能代码的核心** |
| 运行时全局 | `runtime.py` | RUNNER / OUTPUT_BROADCAST / BROWSER 给技能用 |

---

## 四、四种意图路由（规划器的核心）

每次用户说话，规划器（Claude Opus 4.7）会被强制调用四个工具中的一个：

```python
TOOLS = [
    "chat",                    # 不需要做事，直接说一句话
    "call_skill",              # 调用已注册的技能
    "synthesize_one_shot",     # 写个新技能，立即跑一次
    "synthesize_background",   # 写个新后台技能（定时器或视觉监视器）
]
```

规划器会看到三样东西：
1. 用户的转写文本
2. 摄像头当下的图片（如有）
3. **当前技能注册表的简介列表**

第三点是关键 —— **规划器知道哪些技能已经存在**。如果用户说"几点了"且 `current_time` 在列表里，规划器选 `call_skill(current_time)`；如果用户说"打开 HN"且没有相应技能，规划器选 `synthesize_one_shot` 并写一段精确的 NL spec 交给合成器。

---

## 五、动态代码生成的实现细节（核心）

这是 vox 与其他语音助手的根本不同。下面分七步讲清楚。

### 5.1 技能 API 契约

每个技能都是 `skills/*.py` 里的一个 Python 文件，遵守一个非常简单的约定：

**一次性技能：**

```python
RUN_SPEC = {
    "name": "current_time",
    "description": "告诉用户当前时间。无参数。",
    "args_schema": {"type": "object", "properties": {}, "required": []},
}

async def run(**kwargs):
    return {"speak": "现在是 14:32。", "render": "Current time: 2026-06-10 14:32"}


if __name__ == "__main__":
    import asyncio
    r = asyncio.run(run())
    assert isinstance(r, dict) and "speak" in r
    print("OK")
```

**后台技能**（如定时器）：

```python
import runtime

async def run(delay_seconds, message, **kwargs):
    id_ = await runtime.RUNNER.add_timer(float(delay_seconds), message)
    return {"speak": "好的，到时我会提醒你。",
            "render": f"Timer: {id_}"}
```

关键点：
- **形状统一**：所有技能都暴露 `RUN_SPEC` dict + 一个 `async def run(**kwargs)`，返回 `{speak, render}`
- **自带烟雾测试**：每个文件 `python <file>.py` 应该打印 `OK`
- **共享运行时**：技能通过 `import runtime` 访问 BrowserHost、BackgroundRunner、输出广播

合成器写出的代码必须严格符合这个契约，否则会被拒绝。

### 5.2 合成器接收的 spec

规划器调用 `synthesize_one_shot` 时，会输出一段精心生成的自然语言规格说明，例如用户说"给我讲个动物冷知识"时实际看到的 spec：

> "Tell the user a single random interesting fact about an animal. Pick an animal at random (mammal, bird, reptile, fish, insect, etc.) and speak one short, surprising, true fact about it in 1-2 sentences. No preamble, just deliver the fact aloud via TTS."

这段 spec 已经包含：触发条件、行为描述、输出风格。这是规划器（Claude）替合成器（也是 Claude）翻译出的清晰任务定义。

### 5.3 启动合成沙箱

合成器（`synthesizer.py`）做的事：

```python
scratch = SKILLS_DIR / "_scratch" / uuid.uuid4().hex[:12]
scratch.mkdir()                       # 新的、隔离的工作目录

options = ClaudeAgentOptions(
    system_prompt=_build_system_prompt(kind, spec),  # 包含技能 API 和例子
    cwd=str(scratch),                 # Claude 只能在这里读写
    allowed_tools=["Write", "Read", "Edit", "Bash"],
    disallowed_tools=["WebFetch", "WebSearch"],  # 不让上网
    permission_mode="bypassPermissions",          # 无人值守，不弹窗
    max_turns=12,                     # 防止无限循环
    max_budget_usd=0.50,              # 硬成本上限
    env={"ANTHROPIC_BASE_URL": ..., "ANTHROPIC_API_KEY": ...},  # 透传代理
    model="claude-opus-4-7",
)

async for msg in query(prompt=spec, options=options):
    # 收集助手消息、最终 ResultMessage、成本
    ...
```

注意几个**沙箱与控制点**：
- **cwd 限定** —— 文件操作默认相对此目录
- **工具白名单** —— Web 工具被禁用
- **bypassPermissions** —— 无人值守模式
- **预算上限** —— 跑超就直接终止
- **回合上限** —— 防止 Claude 陷入死循环
- **环境变量显式传递** —— ANTHROPIC_BASE_URL 让子进程也走 limtok 代理

### 5.4 合成器的系统提示词

提示词嵌入了**技能 API 的完整契约**和**一个可工作的范例**：

```
你将在当前工作目录写恰好一个 Python 文件，用于 vox agent。

约束:
1. 文件名 = RUN_SPEC.name（snake_case）.py
2. 只用标准库 + 父 venv 已有的包（httpx、anthropic、runtime）
3. 必须包含可独立运行的烟雾测试
4. 写完后 Bash 跑 `python <文件>.py`，看到 "OK" 才算成功
5. 失败重试 3 次
6. 完成后回复 "SYNTHESIS_COMPLETE <文件名>.py"

KIND: {kind}  (one_shot / background_timer / background_vision)

完整可参考的工作范例:
{example_skill_source}

vox 技能 API 总览:
{api_summary}

开始合成。
```

**针对浏览器类技能**，提示词会换成另一个 worked example（用 `runtime.new_page()` 驱动 Playwright），并明确告诉合成器：浏览器技能的烟雾测试**不要调用 run()**，因为合成阶段没有运行中的 Chromium。

### 5.5 双重校验：AST + 独立烟雾测试

Claude 写完文件后，合成器**不轻信**它，做两道独立验证：

**第一道 AST 校验：**

```python
tree = ast.parse(skill_file.read())
# 必须存在 RUN_SPEC 字典
# 必须存在 async def run
# RUN_SPEC.name 必须是合法的 snake_case
```

**第二道烟雾测试（在独立子进程跑一次）：**

```python
subprocess.run(
    ["python3", str(skill_path)],
    cwd=str(scratch),                # 在沙箱内跑
    timeout=30.0,                    # 30 秒超时
    capture_output=True,
    env={...PYTHONPATH=vox_root...}, # 让它能 import runtime
)
# 检查 returncode == 0 且 stdout 包含 "OK"
```

两道都通过才算合格。这是**纵深防御**：Claude 自己也会先跑一遍烟雾测试，但我们不信任它的报告 —— 我们自己在隔离环境再跑一遍。

### 5.6 升级与热重载

通过双重校验后：

```python
final_path = SKILLS_DIR / f"{skill_name}.py"
if final_path.exists():
    return error("skill 已存在；拒绝覆盖")     # 并发安全：第一个赢
shutil.copy2(skill_path, final_path)            # 从沙箱拷贝出来
registry.load_all()                             # 热重载注册表
```

热重载完成的瞬间，新技能就出现在了规划器下次调用时的注册表清单中。**自扩展的闭环就此形成**。

### 5.7 立即调用（一次性技能）

对一次性技能，合成完后**马上调用一次**，这样用户的"给我讲个动物冷知识"在一次交互内既完成了合成又得到了结果：

```
用户：给我讲个动物冷知识
                ↓
规划器：synthesize_one_shot("讲一条随机动物冷知识")
                ↓
合成器：写 random_animal_fact.py（约 20 秒，约 $0.20）
                ↓
合成器：升级到 skills/random_animal_fact.py
                ↓
调度器：调用 random_animal_fact.run() → {speak: "螳螂虾的眼睛有16种色觉感受器…", ...}
                ↓
TTS：朗读
```

**第二次**用户再问"再来一条动物冷知识"时，规划器看到 `random_animal_fact` 已在注册表，直接 `call_skill` —— 不再合成、不再花那 $0.20，约 200ms 完成。这就是**自扩展的成本结构**：每个能力只为它的第一次付费。

---

## 六、关键工程决策

| 决策 | 选择 | 原因 |
|---|---|---|
| 沙箱模型 | 子进程 + 受限 cwd + 工具白名单 + 烟雾测试 | 在轻量与安全之间取平衡；不引入容器复杂度 |
| 合成模型 | Claude Opus 4.7 | 代码质量足以"一次过"；换 Sonnet 可省 3-5 倍成本但质量略降 |
| 视觉触发模型 | Opus 4.7 视觉 | 用户优先选了准确性；预过滤靠 pHash 防爆成本 |
| 帧预过滤 | 感知哈希（pHash） | 几乎免费；静态场景下零调用；活跃场景下也只过滤近似帧 |
| 浏览器 | Playwright 持久化 Chromium | 一次启动多次复用；共享 cookie；headed 模式可视化 |
| 后台事件输出 | WebSocket 推送 + 220ms 提示音 + TTS | 用户选了"打断+提示音"策略 |
| 规划缓存 | 关闭（代理不支持） | 探针验证了 limtok 缓存有创建无读取，会反向涨钱 |

---

## 七、端到端示例

### 例 1：定时器

```
用户："30秒后提醒我喝水"
  ↓ /asr (FunASR, ~700ms)
"30秒后提醒我喝水"
  ↓ /plan
规划器: call_skill(generic_timer, {delay_seconds: 30, message: "该喝水了！"})
  ↓ Dispatcher
runtime.RUNNER.add_timer(30, "该喝水了！")
  ↓ asyncio.create_task(_run_timer)
立即返回 "好的，30秒后提醒你。" → TTS
  ↓ 30 秒后
[bg] FIRE timer_xxxx → output_broadcast({type:"speak", text:"该喝水了！"})
  ↓ /ws/output
前端收到 → 播放 220ms 提示音 → TTS 朗读
```

### 例 2：视觉监视器

```
用户："看到我拿手机时就大声笑"
  ↓
规划器: call_skill(generic_vision_watcher, {
    trigger: "the person is visibly holding a mobile phone…",
    say_on_match: "哈哈哈哈哈！你又在玩手机啦！",
    cooldown_sec: 20
})
  ↓
RUNNER.add_vision_watcher(...)
  ↓
RUNNER.has_vision_watchers() → True
  ↓ output_broadcast({type:"frames_required", value:true})
  ↓
前端开启 /ws/frames，1fps 推帧
  ↓ 后端按帧
pHash 比较前一帧 → 相似则丢弃
否则 → Claude Opus 视觉判断 trigger 是否匹配
匹配 → output_broadcast({type:"speak", text:"哈哈哈哈哈！…"})
  ↓
前端：220ms 提示音 + TTS
```

### 例 3：动态合成 + 立即调用

```
用户："给我讲个动物冷知识"
  ↓
规划器: synthesize_one_shot("讲一条随机动物冷知识…")
  ↓
合成器: Claude Agent SDK 子进程
  - 系统提示词嵌入一次性技能模板
  - cwd = skills/_scratch/4991147432d7/
  - 12 回合上限，$0.5 预算上限
  - Claude 写 random_animal_fact.py（包含 15 个手工挑选的事实）
  - Bash 跑 python random_animal_fact.py → "OK"
  - 回复 SYNTHESIS_COMPLETE random_animal_fact.py
  → ResultMessage: cost=$0.37, stop_reason=end_turn
  ↓
合成器独立校验:
  - AST 检查：RUN_SPEC ✓, async run ✓, name snake_case ✓
  - 烟雾测试：subprocess 跑 30s 内输出 OK ✓
  ↓
升级 skills/random_animal_fact.py + registry.load_all()
  ↓
立即调用: random_animal_fact.run() → {speak: "螳螂虾…", render: "**Mantis shrimp**: …"}
  ↓
返回 /plan 响应 → TTS
```

下次用户说"再来一条"，规划器看见 `random_animal_fact` 已注册，直接 `call_skill`，0 成本、200ms。

---

## 八、成本与延迟（实测）

| 路径 | 延迟 | 成本/次 |
|---|---|---|
| 命中已有一次性技能（如 `current_time`） | ~2.5s（VAD 等待 + ASR + 规划器视觉调用 + TTS） | ~$0.02 |
| 闲聊（路由到 chat） | ~3-4s | ~$0.02 |
| 合成新一次性技能（如动物冷知识） | ~30s | ~$0.10–$0.40 |
| 合成新浏览器技能 | ~30-60s | ~$0.15–$0.50 |
| 视觉监视器（活跃场景） | 持续 | $5–25/小时 |
| 视觉监视器（静态场景，pHash 过滤掉大部分帧） | 持续 | <$1/小时 |

**关键经济学**：合成是一次性投资，每个技能写一次用一辈子。一周内合成 30 个技能 ≈ $10，之后这 30 个能力都是免费调用。

---

## 九、当前状态与未来方向

**已完成**：所有六阶段均工作 —— 语音视觉环路、规划器、Playwright 浏览器、后台运行器（定时器+视觉监视器）、合成器、填充词过滤。

**未做的小优化**：
1. 并发合成去重 —— 用户连说两次"动物冷知识"会跑两次合成
2. 规划器 `say_first` 在前端伪装成监视器触发的 UI 细节
3. 后台技能跨重启持久化（现在重启即清空）
4. 用 `can_use_tool` 回调实现字节级权限拒绝（比工具白名单更细）

**有意义的扩展方向**：
1. **技能版本化** —— 当用户对生成的代码不满意时，让 Claude 修改而不是重写
2. **技能间组合** —— 让规划器调用一个"宏技能"，它内部串联多个原子技能
3. **持久化记忆** —— 让技能能跨会话保存状态
4. **多模态合成示范** —— 在合成器系统提示词中加入处理图像、音频的范例
